#!/usr/bin/env python
while True:
    user_input = input("('STOP' to exit) ")
    if user_input.upper() == "STOP":
        print("Exiting")
        break
    else:
        print(f"entered {user_input}")

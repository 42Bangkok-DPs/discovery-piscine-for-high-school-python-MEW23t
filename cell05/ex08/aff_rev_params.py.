#!/usr/bin/env python
import sys

def main():
    args = sys.argv[1:]
    
    if len(args) < 2:
        print("none")
    else:
        reversed_args = "\n".join(reversed(args))
        print(reversed_args)

if __name__ == "__main__":
    main()
